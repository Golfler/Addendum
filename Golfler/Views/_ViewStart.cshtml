@{
    Layout = "~/Views/Shared/_LayoutAdminArea.cshtml";
}

<script>
    $(document).ready(function () {
        //var page = location.pathname.toLowerCase();
        //page = page.replace('/appstore', '');
        //page = page.replace('/admin', '');
        //page = page.replace('/organization', '');
        //page = page.replace('/developer', '');
        //page = page.replace('/advertiser', '');

        //page = page.replace('/' + $.trim($("#hdOrg").val()).toLowerCase(), '')
        //if (page == '')
        //    page = 'login';
        //$('body').addClass(page.replace('/', ''));
    });
</script>